# Certifications asset placeholders

Add certification badge PNGs and large cover images (WebP/AVIF) for each entry in `data/certs.json`.
Ensure badge images are optimized for 64×64 display and large images remain under 150 KB.
